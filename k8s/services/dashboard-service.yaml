apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: zlinebot
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
