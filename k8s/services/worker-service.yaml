apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: zlinebot
spec:
  selector:
    app: worker
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
